A DIRECTED TYPE THEORY FOR FORMAL CATEGORY
THEORY
DAN LICATA, ANDREAS NUYTS, PATRICK SCHULTZ, AND MICHAEL SHULMAN

1. Overview
We take the point of view that each judgment in a type theory corresponds to a
“sort” of object or morphism in a categorical structure, with individual such objects
or morphisms represented by particular derivation trees of that judgment. More
precisely, if the judgments can be stratified into “levels”, such as types, terms, and
equalities in finite-product theories or λ-calculus, or types, terms, propositions,
and entailments in first-order logic, then each level describes a many-sorted free
algebraic theory (“free” meaning there are no axioms) whose sorts are the possible
judgments and whose operations are the rules. The free categorical structure generated by the type theory is then composed of the elements of the initial algebras
for these algebraic theories, which can be constructed as labeled trees; these are
the derivation trees of type theory.
In general, the judgments of one level — which is to say, the sorts of one of these
algebraic theories — will be defined in terms of the elements of the initial algebras
at previous levels. For instance, in λ-calculus the first level is the types, where
there is only one sort (types) and the basic operation is the binary one taking A
and B to the function type A → B. Then the next level corresponds to morphisms,
in which the judgments (sorts) are of the form A ⊢ B where A and B are types,
i.e. elements of the free algebra generated by the function type operator (plus base
types or type operations).
The role of a term — by which I include variables assigned to types in a context,
and also the name of a type itself — is to uniquely identify a derivation tree with a
conciser syntax. (This tends to be especially important when those derivation trees
are being used as part of a judgment at the next level.) The process of (perhaps)
reconstructing a derivation tree from a term is type-checking.
In the present theory we have four kinds of judgment; here they are expressed
both with and without terms:
Without term
cat
Ψ⊢A
Ψ ⊢ type
∆|Γ⊢M

With term
A cat
Ψ⊢a:A
Ψ ⊢ M type
∆|Γ⊢m:M

There is some abuse of notation here regarding contexts. In the left column, the
contexts Ψ, ∆, Γ denote simple lists of cats or types perhaps with variance; but in
the right column, these same contexts must be annotated somehow with variables.
(As usual, Ψ denotes a list of cats with variance and Γ a list of types. We will
discuss what ∆ denotes in ??.)
1

2

DAN LICATA, ANDREAS NUYTS, PATRICK SCHULTZ, AND MICHAEL SHULMAN

The question of how exactly to annotate them with variables is tricky and we
came up with lots of ways to do it, particularly in the last judgment. But the
point here is that the primary theory ought to be expressed in terms of the left
column. Of course, in order to describe the rules for a later kind of judgment such
as ∆ | Γ ⊢ M , we have to make use of some term syntax for the previous levels; but
mathematically, those rules are defined independently of whatever term syntax we
might use, as long as that syntax is adequate to represent the relevant derivations.
In general, there might be multiple ways of annotating the same derivation trees
by terms; any such annotation is correct as long as a term uniquely specifies a
derivation tree. In fact, in our situation there is a “direct” way to annotate by
terms, which leads to a “string diagram” style calculus, and also a “fancier” way
to annotate that leads to more congenial terms. But the underlying type theory,
meaning the rules and hence the derivation trees and hence the free categorical
structure they generate, is the same in both cases.
2. Cats and their terms
The type theory of cats and their terms is an ordinary “linear type theory” with
two twists. The first is that, of course, we carry variance through everywhere. The
second is that in order to make terms uniquely determine a derivation — and also
to make the derivations describe exactly the morphisms in a free category — we
make the exchange rule admissible rather than primitive. For if exchange were
primitive, then the following two distinct derivations would yield the same term,
and also ought to represent the same morphism:

x : A, y : B ⊢ f (x, y) : C

x : A, y : B ⊢ f (x, y) : C
exchange
y : B, x : A ⊢ f (x, y) : C
exchange
x : A, y : B ⊢ f (x, y) : C

To make exchange admissible, we need to build a certain amount of permutations
into the primitive rules. Not arbitrary permutations, but “just enough” to obtain
any desired ordering of the context of the conclusion without introducing the above
sort of redundancy. The redundancy comes from the fact that the contexts of
the premises must already be free to occur in any order. Thus, we don’t want to
re-build-in permutations of those, only permutations that alter the relative order
between the contexts of different premises. Formally, what we need is a shuffle.
Definition
1. For p1 , . . . , pn ∈ N, a (p1 , . . . , pn )-shuffle is a permutation of
Fn
i=1 {1, . . . , pi } with the property that it leaves invariant the internal ordering
of each summand.
For instance, if we write {1, 2} ⊔ {1, 2, 3} as {1, 2, 1′ , 2′ , 3′ }, then here are some
(2, 3)-shuffles:
121′ 2′ 3′

11′ 2′ 23′

1′ 2′ 13′ 2

1′ 12′ 3′ 2

In all cases 1 comes before 2, and also 1′ comes before 2′ which comes before 3′ .
The name “shuffle”, of course, comes from the fact that when n = 2 this is exactly
the sort of permutation that can be obtained by cutting a deck of p + q cards into
a p-stack and a q-stack and riffle-shuffling them together.
Now let Sp denote the symmetric group on p elements; thus the (p1 , . . . , pn )shuffles are elements of Sp1 +···+pn . Note that they are not a subgroup. However, we

A DIRECTED TYPE THEORY FOR FORMAL CATEGORY THEORY

3

do have a (non-normal) inclusion Sp1F× · · · × Spn ,→ Sp1 +···+pn given by the block
n
sum of permutations, acting on i=1 {1, . . . , pi } by permuting each summand
individually. The following is straightforward to verify.
Lemma 1. Every coset of Sp1 × · · · × Spn in Sp1 +···+pn contains exactly one
(p1 , . . . , pn )-shuffle. Thus, every permutation of p1 +· · ·+pn can be written uniquely
as the product of a block sum from Sp1 × · · · × Spn and a (p1 , . . . , pn )-shuffle. □
If Ψi is a context of length pi for i = 1, . . . , n, then we write Shuf(Ψ1 , . . . , Ψn ; Ψ)
for the set of (p1 , . . . , pn )-shuffles that act on the concatenated context Ψ1 , . . . , Ψn
to produce the context Ψ. When using named variables, we assume that the variable
names are preserved by this action (which means that the contexts Ψ1 , . . . , Ψn and
Ψ uniquely determine such a shuffle if it exists).
Now we state the rules for this type theory. As base/generating data, suppose
we are given a “signed multigraph” G of generators, consisting of a collection of
objects (generating cats) and a collection of arrows (function symbols), where each
arrow has a signed list of objects as its domain and a single object as its codomain.
We write G(Aε11 , . . . , Aεnn ; B) for the set of arrows from (Aε11 , . . . , Aεnn ) to B.
The first judgment “A cat” has only one sort as an algebraic theory; here A is
playing the role of a term. We aren’t currently assuming any operations on cats,
so there is only one trivial rule:
A∈G
A cat
Now the second level has potential judgments (sorts in the algebraic theory) of the
form Aε11 , . . . , Aεnn ⊢ B, where the Ai and B are cats, and the εi ’s are either + or
−. The primitive rules, stated without terms, are:
A cat
A+ ⊢ A

f ∈ G(Aε11 , . . . , Aεnn ; B)

Ψi ⊢ Ai
Φ⊢B

Shuf(Ψε11 , . . . Ψεnn ; Φ)

Here Ψε is defined by Ψ+ = Ψ, whereas Ψ− is Ψ with all variances flipped. With
terms, these rules become:
A cat
x : A+ ⊢ x : A
f ∈ G(Aε11 , . . . , Aεnn ; B)
Ψi ⊢ a1 : Ai
Shuf(Ψε11 , . . . Ψεnn ; Φ)
Φ ⊢ f (a1 , . . . , an ) : B
Lemma 2. Exchange is admissible: if we have a derivation of Ψ ⊢ A, and a
permutation Ψ ∼
= Ψ′ , then we can construct a derivation of Ψ′ ⊢ A. Moreover, this
operation is a group action.
Proof. Induct on the derivation of Ψ ⊢ A. The interesting case is the second
rule. Given its premises and a permutation Φ ∼
= Φ′ , we decompose the composite
ε1
εn ∼
′
∼
permutation Ψ1 , . . . , Ψn = Φ = Φ uniquely as the block sum of n permutations
ε
ε
Ψi ∼
= Ψ′i with a shuffle Ψ′1 1 , . . . , Ψ′n n ∼
= Φ′ . Now by the inductive hypothesis we
′
can derive Ψi ⊢ Ai for all i, whence applying the same rule again with the shuffle
(Ψ′1 , . . . , Ψ′n ) ∼
= Φ′ we get Φ′ ⊢ B. We likewise show that it is a group action by
induction.
□

4

DAN LICATA, ANDREAS NUYTS, PATRICK SCHULTZ, AND MICHAEL SHULMAN

Note that the shuffles appearing in the premises are not notated explicitly in
the terms! Nevertheless, terms still uniquely determine derivations, because we can
inspect the order that the variables appear in a term. First we observe that the
type theory is linear.
Lemma 3. If Ψ ⊢ a : A is derivable, then every variable in Ψ appears exactly once
in a.
□
Lemma 4. If a is a term such that Φ ⊢ a : A is derivable, then it has a unique
derivation.
Proof. By induction on derivations. The outer operation in a term specifies which
arrow in G must have been used in the final rule, so the question is whether this
also determines all the premises uniquely. Inspecting a conclusion such as Φ ⊢
f (a1 , . . . , an ) : B, we see that each variable in Φ must occur in exactly one of the
terms ai . Thus, it must be that each context Ψi in the premises consists exactly of
those variables occurring in ai , and the εi ’s are determined by the arrow f as well.
Moreover, since the shuffle Shuf(Ψε11 , . . . Ψεnn ; Φ) cannot alter the relative order of
variables in Ψi , it must be that the variables in Ψi occur in the same order as they
do in Φ. Thus the premises Ψi ⊢ ai : Ai are uniquly determined. Finally, since
the contexts Ψi and Φ and the εi ’s are now known, the shuffle is also uniquely
determined.
□
Lemma 5. Cut is admissible: given derivations of Ψ, Aε , Ψ′ ⊢ B and Φ ⊢ A, we
can construct a derivation of Ψ, Φε , Ψ′ ⊢ B.
Proof. Induction on the derivation of Ψ, Aε , Ψ′ ⊢ B. If it is the identity, then ε = +
and B = A, so the given Φ ⊢ A is what we want. If it is obtained from a generator,
then we inductively cut Φ ⊢ A into the premise of that rule corresponding to A,
then re-apply the generator rule.
□
This lemma can be taken as defining the operation of substitution on terms:
x[c/x] = c
f (a1 , . . . , an )[c/x] = f (a1 , . . . , ai [c/x], . . . , an )
where in the second rule ai is the unique one of the a’s in which the variable x
occurs.
3. The type judgment
For the third level of types, let us assume that we have given, in addition to our
signed multigraph G, a set of “base types” each with a specified signed list of cats
as domain; we write T (Aε11 , . . . , Aεnn ) for the set of types with this domain. The
sorts of judgment are of the form Aε11 , . . . , Aεnn ⊢ M type, where Ai are cats and
εi ∈ {+, −}. Here M is actually a “term” describing the derivation tree that could
be omitted from the rules, but we don’t bother to write the rules without it.
We start in Figure ?? with rules without built-in substitutions and permutations, and also without variables associated to cats. In Figure ?? we add in the
substitutions and permutations; then finally in Figure ?? we add back the variables
and terms. For simplicity, we start with the “standard” convention whereby every
cat in a context is assigned a unique variable. Thus, the end and coend types have
to bind two variables, as in the “string diagrams” approach. This way, it is easy to

A DIRECTED TYPE THEORY FOR FORMAL CATEGORY THEORY

H ∈ T (Aε11 , . . . , Aεnn )
H
Aε11 , . . . , Aεnn ⊢ type
Ψ1 ⊢ M1 type
Ψ2 ⊢ M2 type
⊗
Ψ1 , Ψ2 ⊢ type

A− , A+ ⊢ type

5

hom

Ψ∗1 ⊢ M1 type
Ψ2 ⊢ M2 type
⊸
Ψ1 , Ψ2 ⊢ type

Ψ, A+ , A− ⊢ M type R
2
Ψ ⊢ type

Ψ, A+ , A− ⊢ M type R 2
Ψ ⊢ type

Figure 1. The type judgment, without substitutions, permutations, or variables
H ∈ T (Aε11 , . . . , Aεnn )

Ψ1 ⊢ A

Ψi ⊢ Ai
Φ ⊢ type

Shuf(Ψε11 , . . . Ψεnn ; Φ)

H

Ψ2 ⊢ A
Shuf(Ψ−
1 , Ψ2 ; Φ)
hom
Φ ⊢ type

Ψ1 ⊢ M1 type

Ψ2 ⊢ M2 type
Φ ⊢ type

Shuf(Ψ1 , Ψ2 ; Φ)

Ψ1 ⊢ M1 type

Ψ2 ⊢ M2 type
Φ ⊢ type

Shuf(Ψ−
1 , Ψ2 ; Φ)

Ψ, A+ , A− ⊢ M type R
2
Ψ ⊢ type

⊗

⊸

Ψ, A+ , A− ⊢ M type R 2
Ψ ⊢ type

Figure 2. The type judgment, with substitutions and permutations, but no variables
show that the syntax of a type uniquely determines its derivation tree, just as we
did for terms in the previous section (Lemmas ?? and ??).
Lemma 6. If Ψ ⊢ M type is derivable, then every variable in Ψ appears exactly
once (free) in M .
□
Lemma 7. If Φ ⊢ M type is derivable, then it has a unique derivation.

□

We then prove the admissibility and functoriality of exchange and substitution
into this judgment. The proof of exchange is just like that for terms in the previous
section. The inductive clauses for substitution of terms are also fairly obvious; they
are shown in Figure ??.
4. Morphisms of types
Last we consider the judgment for type morphisms. This involves an “unsigned”
type context ∆. How can we describe the shape of ∆ without reference to variables? In addition to indicating the cats that the types depend on, ∆ also has to

6

DAN LICATA, ANDREAS NUYTS, PATRICK SCHULTZ, AND MICHAEL SHULMAN

H ∈ T (Aε11 , . . . , Aεnn )
Ψi ⊢ ai : Ai
Shuf(Ψε11 , . . . Ψεnn ; Φ)
Φ ⊢ H(a1 , . . . , an ) type
Ψ1 ⊢ a : A

Ψ2 ⊢ b : A
Shuf(Ψ−
1 , Ψ2 ; Φ)
Φ ⊢ homA (a, b) type

Ψ1 ⊢ M1 type

Ψ2 ⊢ M2 type
Shuf(Ψ1 , Ψ2 ; Φ)
Φ ⊢ M1 ⊗ M2 type

Ψ1 ⊢ M1 type

Ψ2 ⊢ M2 type
Shuf(Ψ−
1 , Ψ2 ; Φ)
Φ ⊢ M1 ⊸ M2 type

Ψ, x : A+ , y : A− ⊢ M type
R
Ψ ⊢ x↷y:A M type

Ψ, x : A+ , y : A− ⊢ M type
R x↷y:A
Ψ⊢
M type

Figure 3. The type judgment, with substitutions, permutations,
and variables with double-binding (co)ends

homA (a, b)[c/x] = homA (a[c/x], b)

(x ∈ a)

homA (a, b)[c/x] = homA (a, b[c/x])

(x ∈ b)

(M ⊗ N )[c/x] = (M [c/x] ⊗ N )

(x ∈ M )

(M ⊗ N )[c/x] = (M ⊗ N [c/x])

(x ∈ N )

(M ⊸ N )[c/x] = (M [c/x] ⊸ N )

(x ∈ M )

(M ⊸ N )[c/x] = (M ⊸ N [c/x])
R
( x↷y:A N )[M/z] = x↷y:A (N [M/z])
R x↷y:A
R x↷y:A
(
N )[M/z] =
(N [M/z])

(x ∈ N )

R

(x, y ∈
/M

z ̸= x, y)

(x, y ∈
/M

z ̸= x, y)

Figure 4. Substitution into types
indicate how all these dependencies are paired up with opposite variances. Thus,
before introducing variables it is reasonable to represent ∆ as a list of signed types,
together with a “pairing”.
More precisely, the potential judgments at this level (sorts of the algebraic theory) consist of:
(1) A list of derivable type judgments Ψ1 ⊢ M1 type, . . . , Ψn ⊢ Mn type, plus
a last one Φ ⊢ N type.
(2) A partition of the concatenated list Ψ1 , . . . , Ψn , Φ of signed cats into twoelement subsets, in which each cat is matched with another occurrence of
the same cat but with the opposite variance.
The context ∆ must contain the information of the list Ψ1 , . . . , Ψn , Φ together
with this pairing. When we introduce variables, the simplest way to write this is
to assign a distinct variable to each signed type, as usual, and then write an extra

A DIRECTED TYPE THEORY FOR FORMAL CATEGORY THEORY

y ↷ x : A | · ⊢ homA (x, y)

∆, y ↷ x′ : A | Γ ⊢ M
∆, x ↷ x′ : A, y ↷ y ′ : A | Γ, homA (x, y ′ ) ⊢ M

∆ | Γ, M1 , M2 ⊢ N
∆ | Γ, M1 ⊗ M2 ⊢ N

Φ1 ⊢ M1 type
Φ2 ⊢ M2 type
Φ1 ↷ Φ′1 , Φ2 ↷ Φ′2 | M1 [′ ], M2 [′ ] ⊢ M1 ⊗ M2

∆, x : A+ , y : A− | Γ, M ⊢ N
R x↷y:A
∆ | Γ,
M ⊢N

Ψ, x : A+ , y : A− ⊢ M type
R x↷y:A
Ψ ↷ Ψ′ , x′ ↷ y ′ : A | M [′ ] ⊢
M

∆, x : A+ , y : A− | Γ ⊢ M
R
∆ | Γ ⊢ x↷y:A M

Ψ, x : A+ , y : A− ⊢ M type
R
Ψ ↷ Ψ′ , x ↷ y : A | x↷y:A M [′ ] ⊢ M

∆ | Γ, M ⊢ N
∆|Γ⊢M ⊸N

7

Φ1 ⊢ M1 type
Φ2 ⊢ M2 type
′
′
′
Φ1 ↷ Φ1 , Φ2 ↷ Φ2 | M1 [ ], M1 ⊸ M2 [′ ] ⊢ M2

Figure 5. Morphism judgment with no substitutions, permutations, or cuts
list of pairings, such as
x : A+ , y : B − , z : B + , w : A− , x ↷ w, z ↷ y
If we adopt the convention that the first variable listed in each pair always has
variance +, we can write this more concisely as
x ↷ w : A, z ↷ y : B
If Ψ is a signed context, we write Ψ ↷ Ψ′ for the context obtained by duplicating
each variable in Ψ to a primed one in Ψ′ , assigning it opposite variance, and then
pairing each variable with its prime. Thus, if Ψ = (x : A+ , y : B − ), then Ψ ↷ Ψ′ is
(x ↷ x′ : A, y ′ ↷ y : B). In this situation, we write M [′ ] for the result of replacing
all variables in M by their primed versions.
With these conventions, the rules without any substitutions or cuts built in are
shown in Figure ??.
In this syntax, the substitution rule for this judgment should be
Ψ⊢a:A
∆, x ↷ y : A | Γ ⊢ M
∆, Ψ ↷ Ψ′ | Γ[a/x, a′ /y] ⊢ M [a/x, a′ /y]
where a′ denotes the term a with all its variables primed. Building this in in
appropriate places, along with shuffles, we get the rules in Figure ??.
Next we could try to build in enough cuts to make cut admissible in either a
sequent calculus or natural deduction style, but let’s skip that for now.
5. Doubled variables
R x:A
M with the
A nicer-looking syntax for this judgment results from writing
variable x occurring twice in M . Intuitively, this is unambiguous because one of
the occurrences must be covariant and one contravariant. We can formalize it
by generalizing the entire term-assignment system (for both terms and types) to

8

DAN LICATA, ANDREAS NUYTS, PATRICK SCHULTZ, AND MICHAEL SHULMAN

Ψ⊢a:A
Ψ ↷ Ψ | · ⊢ homA (a′ , a)
′

Ψ⊢a:A
Φ⊢b:A
∆, y ↷ x′ : A | Γ ⊢ M
′
′
′
′
∆, Ψ ↷ Ψ , Φ ↷ Φ | Γ[a /x , b/y], homA (a, b′ ) ⊢ M [a′ /x′ , b/y]
∆ | Γ, M1 , M2 ⊢ N
∆ | Γ, M1 ⊗ M2 ⊢ N

Φ1 ⊢ M1 type
Φ2 ⊢ M2 type
′
′
Φ1 ↷ Φ1 , Φ2 ↷ Φ2 | M1 [′ ], M2 [′ ] ⊢ M1 ⊗ M2

∆, x : A+ , y : A− | Γ, M ⊢ N
R x↷y:A
∆ | Γ,
M ⊢N

Ψ, x : A+ , y : A− ⊢ M type
R x↷y:A
Ψ ↷ Ψ′ , Φ ↷ Φ′ | M [′ ][a/y ′ , a′ /x′ ] ⊢
M

∆, x : A+ , y : A− | Γ ⊢ M
R
∆ | Γ ⊢ x↷y:A M

Φ⊢a:A
Ψ, x : A+ , y : A− ⊢ M type
R
Ψ ↷ Ψ′ , Φ ↷ Φ′ | x↷y:A M [′ ] ⊢ M [a/x, a′ /y]

∆ | Γ, M ⊢ N
∆|Γ⊢M ⊸N

Φ⊢a:A

Φ1 ⊢ M1 type
Φ2 ⊢ M2 type
′
′
′
Φ1 ↷ Φ1 , Φ2 ↷ Φ2 | M1 [ ], M1 ⊸ M2 [′ ] ⊢ M2

Figure 6. Morphism judgment with substitutions and permutations, but no cuts
permit contexts in which each variable appears up to twice, as long as any doubleoccurrences are associated to the same cat with opposite variances. Similarly, we
only allow ourselves to concatenate contexts Ψ1 , Ψ2 if the concatenated context
satisfies this invariant.
Note that we are not changing the judgments, rules, or derivation trees; only
the terms assigned to them and the type-checking algorithm. With the above
constraints implicit, the rules for terms and types are unchanged, except for the
end and coend that now bind one variable twice rather than two different variables:
Ψ, x : A+ , x : A− ⊢ M type
R
Ψ ⊢ x:A M type

Ψ, x : A+ , x : A− ⊢ M type
R x:A
Ψ⊢
M type

In general, of course, using the same variable for different types in the context would
usually break the type-checking correspondence between terms and derivations.
The reason it works here is that two occurrences of a variable in a context are
required to be distinguished by their variances, and we can decide by syntactic
analysis of a term or type (before type-checking it to produce a derivation) what
the variance of each free occurrence of a variable therein is.
More formally, we define inductively a notion of “variance” for occurrences of
variables in (possibly ill-typed) terms as follows.
• x occurs with variance + in x.
• If x occurs in ai with variance ε, and f ∈ G(Aε11 , . . . , Aεi i , . . . , Aεnn ; B), then
the induced occurrence of x in f (a1 , . . . , an ) has variance εεi .
• Similarly, if x occurs in ai with variance ε, and H ∈ T (Aε11 , . . . , Aεi i , . . . , Aεnn ),
then the induced occurrence of x in H(a1 , . . . , an ) has variance εεi .

A DIRECTED TYPE THEORY FOR FORMAL CATEGORY THEORY

9

• If x occurs in a with variance ε, then the induced occurrence of x in
homA (a, b) has variance −ε.
• If x occurs in b with variance ε, then the induced occurrence of x in
homA (a, b) has variance ε.
• If x occurs in M1 or M2 with variance ε, then the induced occurrence of x
in M1 ⊗ M2 has variance ε.
• If x occurs in M1 with variance ε, then the induced occurrence of x in
M1 ⊸ M2 has variance −ε.
• If x occurs in M2 with variance ε, then the induced occurrence of x in
M1 ⊸ M2 has variance ε.
• If x occurs in M with variance ε, and x ̸= y, then the induced occurrence
R
R y:A
of x in y:A M and
M has variance ε.
Then we prove the following by induction on derivations. Let J denote either a
judgment b : B or M type.
Lemma 8. If Ψ ⊢ J is derivable, then each variable x : Aε in Ψ occurs exactly
once in J with variance ε. (Thus, if x : A+ and x : A− both appear in Ψ, then x
occurs exactly twice in J , once with each variance.)
This enables us to show:
Lemma 9. If Ψ ⊢ J is derivable, then it has a unique derivation.
Proof. The proof method of ?? (which we also used for ??, without further comment) still works because of Lemma ??. For instance, consider a judgment Ψ ⊢
M ⊗ N type. If this is derivable, then each variable x : Aε in the context occurs
exactly once in M ⊗ N with variance ε, hence in exactly one of M and N with that
variance. (The fact that we might also have x appearing with a different variance
elsewhere doesn’t matter.) Thus, the context Ψ splits uniquely as a shuffle of a
context for M and a context for N , and the induction proceeds as before.
□
We can prove admissibility of exchange as before. Substitution is a little more
subtle to state since variables can occur more than once:
Lemma 10. Let Ψ be a context not containing the variable x at all.
(1) If Ψ, x : Aε ⊢ J and Φ ⊢ a : A, then Ψ, Φε ⊢ J [a/x] (assuming the context
Ψ, Φε is valid).
(2) If Ψ, x : A+ , x : A− ⊢ J and Φ ⊢ a : A, then Ψ, Φ, Φ− ⊢ J [a/x] (assuming
the context Ψ, Φ, Φ− is valid, which means that Φ has no doubled variables
and none of its variable occur in Ψ either).
□
We also need to be able to “coalesce” distinct variables:
Lemma 11. Let Ψ be a context not containing the variables x, y, or z. If we can
derive Ψ, x : A+ , y : A− ⊢ J , then also Ψ, z : A+ , z : A− ⊢ J [z/(x, y)].
□
Here J [z/(x, y)] just means that we substitute z for both x and y; we write
it with a special notation to emphasize that what’s happening is a little different
from a substitution J [z/x, z/y] in ordinary type theory, since z still appears twice
in the context. For brevity, we write J [a/(x, y)] for J [z/(x, y)][a/z]. We also write
J [z/(x, y)] in cases where it is unknown whether both x and y appear in J , with
the intent that if only one appears then we mean J [z/x] or J [z/y], and if none
appears we just mean J .

10

DAN LICATA, ANDREAS NUYTS, PATRICK SCHULTZ, AND MICHAEL SHULMAN

Ψ⊢a:A
0

Ψ | · ⊢ homA (a, a)
Ψ⊢a:A

Φ⊢b:A

∆, z : A | Γ[z/(x, y)] ⊢ M [z/(x, y)]

∆, Ψ0 , Φ0 | Γ[a/x, b/y], homA (a, b) ⊢ M [a/x, b/y]
∆ | Γ, M1 , M2 ⊢ N
∆ | Γ, M1 ⊗ M2 ⊢ N

Φ1 ⊢ M1 type

Φ2 ⊢ M2 type
0
0
Φ1 , Φ2 | M1 , M2 ⊢ M1 ⊗ M2

∆, x : A+ , x : A− | Γ, M ⊢ N
R x:A
∆ | Γ,
M ⊢N

Φ⊢a:A

∆, x : A+ , x : A− | Γ ⊢ M
R
∆ | Γ ⊢ x:A M

Φ⊢a:A

∆ | Γ, M ⊢ N
∆|Γ⊢M ⊸N

Ψ, x : A+ , x : A− ⊢ M type
R x:A
Ψ0 , Φ0 | M [a/x] ⊢
M

Ψ, x : A+ , x : A− ⊢ M type
R
Ψ0 , Φ0 | x:A M ⊢ M [a/x]

Φ1 ⊢ M1 type

Φ2 ⊢ M2 type
0
0
Φ1 , Φ2 | M1 , M1 ⊸ M2 ⊢ M2

Figure 7. Morphism judgment with doubled variables
Now for the morphism judgment, we can take our contexts ∆ to be ordinarylooking ones in which each variable occurs only once, without variances. Implicitly,
we view each such variable as occurring twice, with both variances, and “paired
with itself”. Thus, x : A, y : B will mean something like “x ↷ x : A, y ↷ y : B” in
our previous notation (but of course in ?? we didn’t allow multiple occurrences of
the same variable). The important thing is that with our current term-assignment
system, these contexts carry exactly the same information as the ∆’s with stringdiagram-style pairings from ??.
With this convention, the rules from ?? become those in ??. We write Ψ0 for the
unsigned context obtained from a signed context Ψ by discarding all the variances;
this corresponds to the “doubled” context Ψ ↷ Ψ′ from ??.

